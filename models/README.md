# YOLOv4-tiny Models

Run this command from the repo root to download the model files:

```bash
bash setup/download_models.sh
```

After downloading, this folder should contain:

```text
yolov4-tiny.cfg
yolov4-tiny.weights
coco.names
```
